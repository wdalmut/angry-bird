{
  "name": "angry-corley",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "./node_modules/.bin/browserify src/index.js > bundle.js",
    "dev": "./node_modules/.bin/watchify src/index.js -o dist/bundle.js",
    "serve": "./node_modules/.bin/http-server -p 6000 -c-1 -a 0.0.0.0 dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "matter-js": "^0.18.0"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "http-server": "^14.1.0",
    "watchify": "^4.0.0"
  }
}
